# Deferred Work

Items deferred during reviews. Each entry notes its origin and a one-line reason.

## Deferred from: code review of 1-5-enforce-pkr-only-gateway-eligibility (2026-06-10)

- **No integration coverage of the production `getCurrencies()`→`config.json` wiring** [components/gateways/nonmerchant/kuickpay/tests/KuickPayCurrencyEligibilityTest.php] — the self-contained harness (spec Task 5.2/5.4) stubs `NonmerchantGateway` as an empty class, so the test must override `getCurrencies()` and cannot exercise the framework's `Gateway::loadConfig()`→`$this->config->currencies` path that production actually reads. `testConfigDeclaresOnlyPkr()` covers config.json's *content*, but the content→`getCurrencies()` wiring (inherited Blesta base code) is unverified by this suite. Reason: exercising it requires the absent `../tests` Blesta framework, which is out of this story's scope and spec-prohibited; the override is now documented as load-bearing (commit `96305301`). Revisit when the sibling Blesta PHPUnit suite is available.

## Deferred from: code review of 3-1-wrap-kuickpay-soap-operations (2026-06-09)

- **`isTimeout()` classification is message-text/locale-dependent** [components/gateways/nonmerchant/kuickpay/lib/KuickPaySoapClient.php] — timeout vs transport_error is decided by substring-matching the exception message (`/timeout|timed out|temporarily unavailable/i`). Reason: only the `error_class` *label* is affected — both classes retry identically and neither is a payment signal (AC6), so behavior is correct; revisit if alerting or caller branching ever depends on the exact class.
- **Redaction completeness vs an expanded provider contract** [components/gateways/nonmerchant/kuickpay/lib/KuickPayRedactor.php:77] — `redactEnvelope()` masks element *text* by exact local-name, so sensitive values carried in XML *attributes* or under aliased element names (`CustomerName`, `MobileNo`, CNIC, etc.) are not masked. Reason: not triggerable under the confirmed element-based KuickPay contract (all sensitive fields are scalar elements with the enumerated names); revisit if the provider contract changes.
- **Verification record is internally contradictory; tests never ran under the target toolchain** [_bmad-output/implementation-artifacts/3-1-wrap-kuickpay-soap-operations.md] — the Dev Agent Record Risks say "PHP toolchain / ext-soap CONFIRMED ABSENT" while the Completion note says "PHP CLI 8.3.31 present with dom/libxml/soap loaded" (verified: `php` resolves to 8.3.31 with ext-soap). So `php -l` ran on PHP 8.3, not the 8.2 target, and the PHPUnit suite was only exercised via a "direct PHP fallback script", never an actual PHPUnit runner. Reason: not a code change — a pre-merge verification action: run `php -l` + the component-local suite under PHP 8.2 + PHPUnit ~8.5 and reconcile the contradictory Risk/Completion statements before merge.

## Deferred from: code review of 0-1-confirm-kuickpay-contract-and-capture-sanitized-fixtures (2026-06-09)

- **Fixture coverage gaps for Story 3.2 hardening** [docs/kuickpay/fixtures/] — add fixtures the Story 3.2 parser should be forced to handle but that are beyond this story's AC2-enumerated set: a non-2-char `InsertVoucher` status; an inquiry amount with differing precision/trailing zeros (decimal/minor-unit comparison trap); an empty / short (`<6` field) inquiry result; a non-PKR / empty currency on an otherwise-paid row; a multi-row bulk dataset mixing matched + unmatched + a duplicate Consumer Number; an overpayment and a late/partial (`AmountAfterDueDate`) case; and a Consumer-Number pair where one value is a suffix/substring of another (to prove exact-match discrimination). Reason: out of scope for the AC2 gate set; correct home is the Story 3.2 canonical test tree.
- **`registration_number = random_prefix + invoice_id` idempotency risk** [docs/kuickpay/phase-0-contract.md:27] — a random prefix on the reconciliation reference defeats retry de-duplication and conflicts with the "never blindly retry InsertVoucher" rule, because a retry cannot be recognized as the same voucher. Reason: candidate formula is `UNCONFIRMED`; reference-generation determinism is an Epic 2/3 design decision, not a Phase-0 doc concern.
- **Mixed redaction placeholder style** [docs/kuickpay/testing-fixtures.md:73] — placeholders mix `REDACTED_*` tokens with masked-PII forms (`0300XXXXXXX`, `customer@example.invalid`), so an automated leak-scan keyed on `REDACTED_` would miss the masked values. Reason: cosmetic consistency only; current forbidden-value and redaction scans both pass, so no leak exists today.

## Deferred from: code review of 1-1-install-kuickpay-gateway-and-companion-plugin-scaffold (2026-06-09)

- **Cross-company companion detection on null `company_id`** [components/gateways/nonmerchant/kuickpay/kuickpay.php:246] — `companionInstalled()` calls `PluginManager::isInstalled('kuickpay_reconcile', Configure::get('Blesta.company_id'))`; when `company_id` is null (CLI/cron/early bootstrap), `isInstalled` drops the company filter and matches the plugin under any company (`app/models/plugin_manager.php:214`). Edge Case Hunter rated this High in the abstract; in this scaffold the real impact is nil because no live path is enabled in any branch and the user-facing `getSettings`/`buildProcess` are HTTP-only (company_id always set). Reason: conscious, documented scaffold-scope decision (Dev Notes "isInstalled semantics"); enforce company-scoped + `enabled` state in the story that first exposes a real plugin service/action.
- **Missing gateway logo (broken admin extension-card image)** [components/gateways/nonmerchant/kuickpay/views/default/images/logo.png] — file not present; `GatewayManager::getGatewayInfo()` (`gateway_manager.php:917-919`) builds the logo `<img>` URL unconditionally, so the admin extension card shows a broken image. AC1 detectability unaffected. Reason: spec lists `logo.png` as optional with a sanctioned broken-image tradeoff; supply real branding (or a plain placeholder) in a later pass.

## Deferred from: code review of 1-2-configure-kuickpay-gateway-settings (2026-06-09)

- **Credential re-entry on every save / blank-overwrite risk** [components/gateways/nonmerchant/kuickpay/kuickpay.php:247; components/gateways/nonmerchant/kuickpay/views/default/settings.pdt:37,69] — passwords are required-non-empty AND rendered empty by design (Non-Negotiable #1), so editing any unrelated setting forces re-typing both passwords; a blank submit fails validation and `GatewayManager::setMeta` is delete+insert with no keep-if-blank. Reason: explicitly accepted by Story 1.2 ("Interim credential re-save behavior") and owned by Story 1.3 (keep-if-blank / rotation-on-blank, which needs current-meta plumbing 1.2 does not have).
- **`wsdl_url` HTTPS validation is lenient (SSRF / userinfo)** [components/gateways/nonmerchant/kuickpay/kuickpay.php:106-119] — `FILTER_VALIDATE_URL` accepts internal hosts and embedded credentials (`https://user:pass@host/wsdl`), an SSRF and credentials-in-URL surface once the WSDL is fetched server-side. Reason: the SOAP WSDL fetch is Story 3.1 (Epic 3); host-allowlist and userinfo rejection belong with the consumer that performs the request, not the settings form.
- **Numeric settings lack bounds / relational checks** [components/gateways/nonmerchant/kuickpay/kuickpay.php:102,167-181] — `soap_timeout=0` (instant/no-timeout footgun), unbounded large values, and leading zeros all pass; no `expiry_date_offset_days >= due_date_offset_days` relation is enforced. Reason: the meaningful bounds are defined by the Epic 2/3 consumers (SOAP client timeout; `DueDate`/`ExpiryDate` mapping); AC2 only requires non-negative integers.

## Deferred from: code review of 1-3-encrypt-and-mask-kuickpay-credentials (2026-06-09)

- **`maskCredentials()` primitive mishandles non-array / non-string inputs** [components/gateways/nonmerchant/kuickpay/kuickpay.php:291-294; components/gateways/lib/gateway.php `maskDataRecursive`] — object graphs raise `TypeError` (`str_repeat` over `strlen($object)`); credential keys nested inside objects are never traversed (`maskDataRecursive` recurses only on `is_array`); non-string scalars (null/bool) emit PHP 8.2 deprecations and mask to an empty string. Reason: 1.3 establishes the boundary as a contract with NO live caller, so none of these inputs occur yet; Story 1.4 / Epic 3 callers must pass normalized string arrays (the in-repo `serialize($this->maskData($params, ...))` idiom) or harden the boundary before it logs real SOAP payloads. Verified against base-class behavior by the Edge Case Hunter.
- **Value-based redaction of already-serialized credential strings is out of the array-key boundary** [components/gateways/nonmerchant/kuickpay/kuickpay.php:291-294] — `maskCredentials()` redacts by array key only; a credential concatenated into a URL/query/exception string before becoming an array (e.g. `"user=…pass=…"`) is not caught. Reason: that layer is Epic 3 / Story 3.2's SOAP/XML `redactor` protocol class, intentionally a separate, consistent layer from the gateway-owned credential boundary (architecture.md:397,778).
- **Credential mask allowlist is exact-match & case-sensitive** [components/gateways/nonmerchant/kuickpay/kuickpay.php:21-30] — base-class `array_search` matching is case-sensitive, so common credential spellings (`username`, `Username`, `PASSWORD`, `pwd`, `pin`, `token`, `secret`) are NOT in `$credential_mask_fields` and would pass through unmasked if they ever reach gateway logging; the four stored `gateway_meta` keys are covered. Reason: exact SOAP credential field names are unconfirmed until Phase 0 (0.1); the four stored gateway_meta keys are already covered, and allowlist completion is owned by Story 3.2 per Open Question #4. (Resolved from code-review decision-needed → defer, 2026-06-09.)

## Deferred from: code review of 1-4-run-safe-kuickpay-connection-tests (2026-06-09)

- **SSRF private-range guard — RESOLVED 2026-06-09 (post-review, at user request).** The private/loopback/link-local/reserved-range block (incl. cloud metadata `169.254.169.254`) was implemented in `runConnectionTest()`: host resolution via `resolveProbeAddresses()` (catches `localhost`), `filter_var(..., FILTER_FLAG_NO_PRIV_RANGE | FILTER_FLAG_NO_RES_RANGE)` rejection, unresolvable-host block, and `CURLOPT_RESOLVE` pinning to defeat DNS rebinding (4 new tests). **Residual still deferred** [components/gateways/nonmerchant/kuickpay/kuickpay.php] — the allowlist of *specific* confirmed production KuickPay hosts (do not invent host literals until Phase 0 / Epic 5), and the IPv6-only-DNS gap (`gethostbynamel` resolves IPv4 only). The confirmed-endpoint allowlist supersedes both when Epic 5 lands.
- **HTTP 4xx/5xx/redirect treated as "reachable success"** [components/gateways/nonmerchant/kuickpay/kuickpay.php:353] — `errno===0 && response_code>0` returns success, so a 404 on a mistyped WSDL path (or a 301/500) reports reachable and saves the settings. Reason: deliberate pure-reachability design (Open Question #5) — an auth-protected WSDL legitimately returns 401/403; deeper endpoint-validity belongs with the credentialed safe-op test (Story 5-1).
- **120s probe ceiling can exceed PHP `max_execution_time` → hard 500 instead of friendly timeout** [components/gateways/nonmerchant/kuickpay/kuickpay.php:338] — a 120s synchronous probe can be killed by a lower `max_execution_time` before the `connection.timeout` mapping runs. Reason: the ceiling deliberately matches `KuickPaySoapClient::timeout()`; environmental (server `max_execution_time`).
- **Connection-probe test coverage gaps** [components/gateways/nonmerchant/kuickpay/tests/KuickPayConnectionProbeTest.php] — the `connection.unavailable` (curl-missing) branch, the redirect/404-as-success path, and the validation-fails-with-sentinel skip are untested; the reflection seam stubs `Language::_`, so a missing/misspelled language key would not fail the test. Reason: optional hardening; all four `!error.connection.*` keys verified present today.
- **Story Task 6.4 cites broken runner `-c build/phpunit.xml`** [_bmad-output/implementation-artifacts/1-4-run-safe-kuickpay-connection-tests.md] — PHPUnit resolves `tests/bootstrap.php` relative to `build/` and looks for a missing `build/tests/bootstrap.php`. Reason: pre-existing and documented in project-context.md:72; use `--bootstrap tests/bootstrap.php tests` (green: 28 tests / 136 assertions).

## Deferred from: code review of 2-1-create-durable-customer-voucher-records (2026-06-10)

- **Deterministic reference collides with a non-pending voucher → permanent `not_ready` — RESOLVED 2026-06-10 by Story 2.2.** [plugins/kuickpay_reconcile/lib/KuickPayVoucherReferenceService.php] Story 2.2 replaced the fixed `'0000'` deterministic prefix with a secure random 4-digit prefix plus bounded company-scoped registration/consumer uniqueness pre-checks. A prior non-pending voucher for the same invoice no longer forces the same reference value forever; random regeneration can produce a fresh value, and repeated reference-value collisions fail closed with `uniqueness_exhausted`.
- **Duplicate invoice IDs in `invoice_amounts` abort the whole create** [plugins/kuickpay_reconcile/lib/KuickPayVoucherReferenceService.php:72] — the invoice-link loop does not dedupe; a repeated invoice id violates the `(voucher_id, invoice_id)` unique key, rolls back the voucher, and returns null → silent `not_ready`. Reason: the normal Blesta pay flow passes distinct invoices, and the failure is fail-closed (no bad write); multi-invoice handling is owned by Story 2.4. (Edge Case Hunter, MEDIUM.)
- **Reuse lookup keyed only on the first invoice** [plugins/kuickpay_reconcile/lib/KuickPayVoucherReferenceService.php:42] — `getOrCreateForInvoiceContext` reuses by `invoice_amounts[0]['id']` only, so a multi-invoice payment with a partially overlapping set can reuse/display a voucher whose linked invoices and amount differ from what is being paid. Reason: matches spec Task 3.2 (reuse by first invoice); multi-invoice/active-context correctness is Story 2.4. (Edge Case Hunter + Acceptance Auditor + Blind Hunter, MEDIUM.)
- **No minimum/positive amount check** [plugins/kuickpay_reconcile/models/kuickpay_vouchers.php:225] — the amount rule only checks non-empty + decimal format, so `0` / `0.00` passes and creates a zero-value pending voucher shown to the customer as payable. Reason: amount-change/value gating is Story 2.4; AC1 specifies no minimum. (Edge Case Hunter, LOW.)
- **`KuickpayVouchers::edit()` not company-scoped + dead empty-fields guard** [plugins/kuickpay_reconcile/models/kuickpay_vouchers.php:80] — the UPDATE filters on `id` only (other read methods scope by `company_id`), and `if (empty($fields))` can never be true because `date_updated` is always added. Reason: `edit()` has no caller in 2.1; its signature matches the spec; tenant-scoped edit + guard cleanup are future hardening for the story that first mutates vouchers. (Blind Hunter, LOW.)

## Deferred from: code review of 2-2-generate-registration-number-and-consumer-number (2026-06-10)

- **`expandPattern()` length check counts bytes, not characters** [plugins/kuickpay_reconcile/lib/KuickPayVoucherReferenceService.php:233] — `strlen($expanded) > 64` measures bytes while the `registration_number`/`consumer_number` columns are `varchar(64)` (characters); a multibyte literal pattern or multibyte `institution_id` near the limit would be rejected at ~16–32 chars. Reason: pre-existing from 2.1's identical `strlen` guard; unreachable via the ASCII-enforced pattern charset (`/^[A-Za-z0-9_{}+\-]+$/`) and the numeric institution/invoice/random inputs; fails closed (never corrupts) if ever hit. Switch to `mb_strlen` if a non-ASCII reference component is ever introduced. (Edge Case Hunter, LOW.)
- **`company_id` lookup vs insert value mismatch on a null-company context** [plugins/kuickpay_reconcile/lib/KuickPayVoucherReferenceService.php:59,90] — collision lookups use `(int) ($context['company_id'] ?? 0)` (scope `0`) while the insert persists `$context['company_id'] ?? null`; on a null company id the uniqueness pre-check queries a different scope than the row would write under, then the model rejects the null and `create()` returns null (benign `not_ready`, `lastError` null). Reason: `Configure::get('Blesta.company_id')` is always a valid int in an authenticated HTTP pay flow, so this cannot occur in practice; fails closed with no bad write. Normalize `company_id` once for both lookup and insert if a non-HTTP entry path is ever added. (Edge Case Hunter, LOW.)
- **Throwable-catch and benign null paths record no admin diagnostic** [plugins/kuickpay_reconcile/lib/KuickPayVoucherReferenceService.php:119] — failures routed through the `Throwable` catch, the empty-`invoice_id` early return, and the `create()`-null race-recovery path return null with `lastError` null, so `recordReferenceGenerationFailure()` logs nothing — an admin sees a missing voucher with zero trace for a swallowed DB exception. Reason: **by design** — spec Task 2.4/2.5 explicitly forbids categorizing the catch/benign paths (they are transient `not_ready`, not AC2 generation failures); a durable `voucher.generation_failed` audit event in `kuickpay_audit_events` is owned by Epic 4 / Story 4.5. (Edge Case Hunter, MEDIUM.)
- **4-digit random prefix gives a 10k keyspace** [plugins/kuickpay_reconcile/lib/KuickPayVoucherReferenceService.php:141] — `randomInt()` draws from `random_int(0, 9999)`, so per-invoice the only entropy is 4 digits; under high invoice volume sharing a company scope, regenerate-on-collision retries could become more frequent and a pathological run could exhaust `MAX_REFERENCE_ATTEMPTS`. Reason: informational — reference-value uniqueness still derives primarily from the appended `invoice_id`, the bounded retry fails closed at `uniqueness_exhausted` (no corruption, no duplicate active reference), and the 4-digit width is fixed by the confirmed KuickPay contract. Revisit only if production telemetry shows elevated exhaustion. (Blind Hunter, LOW.)
