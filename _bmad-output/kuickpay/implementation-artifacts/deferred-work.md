# Deferred Work

Items deferred during reviews. Each entry notes its origin and a one-line reason.

## Deferred from: code review of 3-8-verify-payment-safety-contracts (2026-06-11)

- ✅ **CLOSED by Story 5.4** — broadened mobile/CNIC forbidden patterns (international/dashed/spaced mobile + undashed 13-digit CNIC) with paired positive/negative control tests and a diversified clean fixture; leak suite stays green. _Original:_ **Leak-scan PII/credential patterns are narrow and placeholder-keyed** [plugins/kuickpay_reconcile/tests/KuickPaySecretLeakageTest.php:183-202] — the mobile pattern matches only bare `03XXXXXXXXX`, cnic only the dashed form, email allows only `@example.invalid`, and the `<userName>`/`<password>`/`<InstitutionID>` checks are negative-lookahead allow-checks keyed to the exact `REDACTED_*`/`INSTITUTION_ID` placeholders. Reason: fail-safe and green today (all fixtures are clean placeholders), so broadening to alternate PII formats / mixed placeholder styles is a low-priority hardening; doing it now risks false positives on clean fixtures. Revisit when fixtures diversify or a real-secret regression is suspected.
- **Posting-service confirmed→post→rerun call-count not directly asserted** [plugins/kuickpay_reconcile/tests/KuickPayPostingServiceTest.php] — the new two-call test covers an already-`posted` voucher (count stays zero, per Task 4's literal wording); the complementary path — post a `confirmed_unposted` voucher once (exactly one transaction) then re-run and assert no second transaction — is not a dedicated posting-service test. Reason: the `blesta_transaction_id` idempotency guard is already exercised by `testAlreadyPostedVoucherIsNoOpAfterLock` and bulk rerun-idempotency by `KuickPayReconcileServiceTest`; add the post-then-rerun call-count assertion as future hardening.

## Deferred from: development of 3-8-verify-payment-safety-contracts (2026-06-11)

- **Single-inquiry confirmed evidence can carry a null paid date, creating a latent stuck state** [components/gateways/nonmerchant/kuickpay/lib/KuickPayResponseParser.php] — `parseBulk` fails closed when a matched paid row has a missing/unparseable `Transaction_Date`, but the single-inquiry confirmed branch can still produce `confirmed_unposted` with `paidAt() === null`. Posting fails closed (`KuickPayPostingService::validPaidDate`) and creates no Blesta transaction, so this is not a payment-safety escalation. Reason: Story 3.8 is a verification story with no production parser edits; mirror the bulk `missing_paid_date` guard into the single-inquiry confirmed branch in a dedicated follow-up.

## Deferred from: code review of 2-5-display-customer-payment-reference-panel (2026-06-10)

- **Concurrent reconcile can render a just-issued non-pending voucher as `payable`, collapsing the panel to generic `retry_safe` copy** [components/gateways/nonmerchant/kuickpay/kuickpay.php:1093] — `issueVoucherIfNeeded()` re-reads `getLatestByInvoiceId()` after a successful issuance write without re-asserting `status==='pending'`. If a reconcile run flips that row (e.g. to `confirmed_unposted`/`retry`/`manual_review`) between the write and the re-read, the returned voucher carries a non-pending status into `resolveDisplayMode(..., 'issue')` → `payable`; the `.pdt` `payable` arm requires `status==='pending'` and the `status_only` arm requires `display_mode==='status_only'`, so the panel falls through to the generic `Kuickpay.process.retry_safe` message, suppressing the real status. Reason: the re-read predates Story 2.5 (issuance path), the `issue→payable` mapping is the intended Decision-A contract (asserted by `resolveDisplayMode` tests), and the consequence is benign — no payment-state or success-styling leak, generic safe copy only, self-correcting on the next render once the row routes to `block`→`status_only`. Revisit if observed in practice or when Story 3.5 posting makes more non-pending transitions live.

## Deferred from: development of 2-4-gate-changed-amounts-and-multi-invoice-attempts (2026-06-10)

- **Schema-level active-context uniqueness remains a concurrency residual** [plugins/kuickpay_reconcile/kuickpay_reconcile_plugin.php; plugins/kuickpay_reconcile/models/kuickpay_vouchers.php] — Story 2.4 implements amount/mapping gates and deterministic multi-invoice storage at the application layer, but Task 9 was explicitly scope-gated on architect sign-off and the five ACs do not require the schema change. Without `context_key` / `active_context_key` plus a unique key, two concurrent requests for the same invoice set can still both miss the application-level lookup and insert separate pending vouchers. Reason: no architect sign-off was provided for the schema/upgrade version bump in this implementation run; implement the recommended `context_key` + status-derived `active_context_key` unique key in a dedicated schema story or approved follow-up. **CLOSED at the schema layer (Story 5.2, 2026-06-13):** `context_key` + status-derived STORED `active_context_key` + `uniq_kuickpay_vouchers_active_context (company_id, active_context_key)` landed and proven against the real MariaDB stack (`docs/kuickpay/active-context-concurrency-verification.md`). Production un-gating of `replace`/`allow` additionally requires the 5.5 `retireVoucher()` affected-row hardening (separate item below) before it is safe to flip.

## Deferred from: code review of 1-5-enforce-pkr-only-gateway-eligibility (2026-06-10)

- **No integration coverage of the production `getCurrencies()`→`config.json` wiring** [components/gateways/nonmerchant/kuickpay/tests/KuickPayCurrencyEligibilityTest.php] — the self-contained harness (spec Task 5.2/5.4) stubs `NonmerchantGateway` as an empty class, so the test must override `getCurrencies()` and cannot exercise the framework's `Gateway::loadConfig()`→`$this->config->currencies` path that production actually reads. `testConfigDeclaresOnlyPkr()` covers config.json's *content*, but the content→`getCurrencies()` wiring (inherited Blesta base code) is unverified by this suite. Reason: exercising it requires the absent `../tests` Blesta framework, which is out of this story's scope and spec-prohibited; the override is now documented as load-bearing (commit `96305301`). Revisit when the sibling Blesta PHPUnit suite is available.

## Deferred from: code review of 3-1-wrap-kuickpay-soap-operations (2026-06-09)

- ✅ **CLOSED by Story 5.4** — `isTimeout()` now classifies on attempt duration (≈ configured `timeout()` ceiling) as the primary, locale-independent signal, with the English markers as a secondary fallback. _Original:_ **`isTimeout()` classification is message-text/locale-dependent** [components/gateways/nonmerchant/kuickpay/lib/KuickPaySoapClient.php] — timeout vs transport_error is decided by substring-matching the exception message (`/timeout|timed out|temporarily unavailable/i`). Reason: only the `error_class` *label* is affected — both classes retry identically and neither is a payment signal (AC6), so behavior is correct; revisit if alerting or caller branching ever depends on the exact class.
- ✅ **CLOSED by Story 5.4** — `redactEnvelope()` now masks sensitive XML attribute values too and covers the confirmed aliases (`CustomerName`, `MobileNo`, `CNIC`). _Original:_ **Redaction completeness vs an expanded provider contract** [components/gateways/nonmerchant/kuickpay/lib/KuickPayRedactor.php:77] — `redactEnvelope()` masks element *text* by exact local-name, so sensitive values carried in XML *attributes* or under aliased element names (`CustomerName`, `MobileNo`, CNIC, etc.) are not masked. Reason: not triggerable under the confirmed element-based KuickPay contract (all sensitive fields are scalar elements with the enumerated names); revisit if the provider contract changes.
- **Verification record is internally contradictory; tests never ran under the target toolchain** [_bmad-output/kuickpay/implementation-artifacts/3-1-wrap-kuickpay-soap-operations.md] — the Dev Agent Record Risks say "PHP toolchain / ext-soap CONFIRMED ABSENT" while the Completion note says "PHP CLI 8.3.31 present with dom/libxml/soap loaded" (verified: `php` resolves to 8.3.31 with ext-soap). So `php -l` ran on PHP 8.3, not the 8.2 target, and the PHPUnit suite was only exercised via a "direct PHP fallback script", never an actual PHPUnit runner. Reason: not a code change — a pre-merge verification action: run `php -l` + the component-local suite under PHP 8.2 + PHPUnit ~8.5 and reconcile the contradictory Risk/Completion statements before merge.

## Deferred from: code review of 0-1-confirm-kuickpay-contract-and-capture-sanitized-fixtures (2026-06-09)

- **Fixture coverage gaps for Story 3.2 hardening** [docs/kuickpay/fixtures/] — add fixtures the Story 3.2 parser should be forced to handle but that are beyond this story's AC2-enumerated set: a non-2-char `InsertVoucher` status; an inquiry amount with differing precision/trailing zeros (decimal/minor-unit comparison trap); an empty / short (`<6` field) inquiry result; a non-PKR / empty currency on an otherwise-paid row; a multi-row bulk dataset mixing matched + unmatched + a duplicate Consumer Number; an overpayment and a late/partial (`AmountAfterDueDate`) case; and a Consumer-Number pair where one value is a suffix/substring of another (to prove exact-match discrimination). Reason: out of scope for the AC2 gate set; correct home is the Story 3.2 canonical test tree.
- **`registration_number = random_prefix + invoice_id` idempotency risk** [docs/kuickpay/phase-0-contract.md:27] — a random prefix on the reconciliation reference defeats retry de-duplication and conflicts with the "never blindly retry InsertVoucher" rule, because a retry cannot be recognized as the same voucher. Reason: candidate formula is `UNCONFIRMED`; reference-generation determinism is an Epic 2/3 design decision, not a Phase-0 doc concern.
- ✅ **CLOSED by Story 5.4** — the leak-scan now tolerates mixed placeholder styles via a negative-control test and a diversified clean fixture, while still catching real masked-PII formats. _Original:_ **Mixed redaction placeholder style** [docs/kuickpay/testing-fixtures.md:73] — placeholders mix `REDACTED_*` tokens with masked-PII forms (`0300XXXXXXX`, `customer@example.invalid`), so an automated leak-scan keyed on `REDACTED_` would miss the masked values. Reason: cosmetic consistency only; current forbidden-value and redaction scans both pass, so no leak exists today.

## Deferred from: code review of 1-1-install-kuickpay-gateway-and-companion-plugin-scaffold (2026-06-09)

- **Cross-company companion detection on null `company_id`** [components/gateways/nonmerchant/kuickpay/kuickpay.php:246] — `companionInstalled()` calls `PluginManager::isInstalled('kuickpay_reconcile', Configure::get('Blesta.company_id'))`; when `company_id` is null (CLI/cron/early bootstrap), `isInstalled` drops the company filter and matches the plugin under any company (`app/models/plugin_manager.php:214`). Edge Case Hunter rated this High in the abstract; in this scaffold the real impact is nil because no live path is enabled in any branch and the user-facing `getSettings`/`buildProcess` are HTTP-only (company_id always set). Reason: conscious, documented scaffold-scope decision (Dev Notes "isInstalled semantics"); enforce company-scoped + `enabled` state in the story that first exposes a real plugin service/action.
- **Missing gateway logo (broken admin extension-card image)** [components/gateways/nonmerchant/kuickpay/views/default/images/logo.png] — file not present; `GatewayManager::getGatewayInfo()` (`gateway_manager.php:917-919`) builds the logo `<img>` URL unconditionally, so the admin extension card shows a broken image. AC1 detectability unaffected. Reason: spec lists `logo.png` as optional with a sanctioned broken-image tradeoff; supply real branding (or a plain placeholder) in a later pass.

## Deferred from: code review of 1-2-configure-kuickpay-gateway-settings (2026-06-09)

- **Credential re-entry on every save / blank-overwrite risk** [components/gateways/nonmerchant/kuickpay/kuickpay.php:247; components/gateways/nonmerchant/kuickpay/views/default/settings.pdt:37,69] — passwords are required-non-empty AND rendered empty by design (Non-Negotiable #1), so editing any unrelated setting forces re-typing both passwords; a blank submit fails validation and `GatewayManager::setMeta` is delete+insert with no keep-if-blank. Reason: explicitly accepted by Story 1.2 ("Interim credential re-save behavior") and owned by Story 1.3 (keep-if-blank / rotation-on-blank, which needs current-meta plumbing 1.2 does not have).
- **`wsdl_url` HTTPS validation is lenient (SSRF / userinfo)** [components/gateways/nonmerchant/kuickpay/kuickpay.php:106-119] — `FILTER_VALIDATE_URL` accepts internal hosts and embedded credentials (`https://user:pass@host/wsdl`), an SSRF and credentials-in-URL surface once the WSDL is fetched server-side. Reason: the SOAP WSDL fetch is Story 3.1 (Epic 3); host-allowlist and userinfo rejection belong with the consumer that performs the request, not the settings form.
- **Numeric settings lack bounds / relational checks** [components/gateways/nonmerchant/kuickpay/kuickpay.php:102,167-181] — `soap_timeout=0` (instant/no-timeout footgun), unbounded large values, and leading zeros all pass; no `expiry_date_offset_days >= due_date_offset_days` relation is enforced. Reason: the meaningful bounds are defined by the Epic 2/3 consumers (SOAP client timeout; `DueDate`/`ExpiryDate` mapping); AC2 only requires non-negative integers.

## Deferred from: code review of 1-3-encrypt-and-mask-kuickpay-credentials (2026-06-09)

- ✅ **CLOSED by Story 5.4** — `maskCredentials()` reimplemented gateway-local (base class untouched): case-insensitive allowlist, null preserved, array/non-stringable object collapsed to a token, scalar cast then masked — no `TypeError`/deprecation. _Original:_ **`maskCredentials()` primitive mishandles non-array / non-string inputs** [components/gateways/nonmerchant/kuickpay/kuickpay.php:291-294; components/gateways/lib/gateway.php `maskDataRecursive`] — object graphs raise `TypeError` (`str_repeat` over `strlen($object)`); credential keys nested inside objects are never traversed (`maskDataRecursive` recurses only on `is_array`); non-string scalars (null/bool) emit PHP 8.2 deprecations and mask to an empty string. Reason: 1.3 establishes the boundary as a contract with NO live caller, so none of these inputs occur yet; Story 1.4 / Epic 3 callers must pass normalized string arrays (the in-repo `serialize($this->maskData($params, ...))` idiom) or harden the boundary before it logs real SOAP payloads. Verified against base-class behavior by the Edge Case Hunter.
- **Value-based redaction of already-serialized credential strings is out of the array-key boundary** [components/gateways/nonmerchant/kuickpay/kuickpay.php:291-294] — `maskCredentials()` redacts by array key only; a credential concatenated into a URL/query/exception string before becoming an array (e.g. `"user=…pass=…"`) is not caught. Reason: that layer is Epic 3 / Story 3.2's SOAP/XML `redactor` protocol class, intentionally a separate, consistent layer from the gateway-owned credential boundary (architecture.md:397,778).
- **Credential mask allowlist is exact-match & case-sensitive** [components/gateways/nonmerchant/kuickpay/kuickpay.php:21-30] — base-class `array_search` matching is case-sensitive, so common credential spellings (`username`, `Username`, `PASSWORD`, `pwd`, `pin`, `token`, `secret`) are NOT in `$credential_mask_fields` and would pass through unmasked if they ever reach gateway logging; the four stored `gateway_meta` keys are covered. Reason: exact SOAP credential field names are unconfirmed until Phase 0 (0.1); the four stored gateway_meta keys are already covered, and allowlist completion is owned by Story 3.2 per Open Question #4. (Resolved from code-review decision-needed → defer, 2026-06-09.)

## Deferred from: code review of 1-4-run-safe-kuickpay-connection-tests (2026-06-09)

- **SSRF private-range guard — RESOLVED 2026-06-09 (post-review, at user request).** The private/loopback/link-local/reserved-range block (incl. cloud metadata `169.254.169.254`) was implemented in `runConnectionTest()`: host resolution via `resolveProbeAddresses()` (catches `localhost`), `filter_var(..., FILTER_FLAG_NO_PRIV_RANGE | FILTER_FLAG_NO_RES_RANGE)` rejection, unresolvable-host block, and `CURLOPT_RESOLVE` pinning to defeat DNS rebinding (4 new tests). **Residual still deferred** [components/gateways/nonmerchant/kuickpay/kuickpay.php] — the allowlist of *specific* confirmed production KuickPay hosts (do not invent host literals until Phase 0 / Epic 5), and the IPv6-only-DNS gap (`gethostbynamel` resolves IPv4 only). The confirmed-endpoint allowlist supersedes both when Epic 5 lands.
- **HTTP 4xx/5xx/redirect treated as "reachable success"** [components/gateways/nonmerchant/kuickpay/kuickpay.php:353] — `errno===0 && response_code>0` returns success, so a 404 on a mistyped WSDL path (or a 301/500) reports reachable and saves the settings. Reason: deliberate pure-reachability design (Open Question #5) — an auth-protected WSDL legitimately returns 401/403; deeper endpoint-validity belongs with the credentialed safe-op test (Story 5-1).
- **120s probe ceiling can exceed PHP `max_execution_time` → hard 500 instead of friendly timeout** [components/gateways/nonmerchant/kuickpay/kuickpay.php:338] — a 120s synchronous probe can be killed by a lower `max_execution_time` before the `connection.timeout` mapping runs. Reason: the ceiling deliberately matches `KuickPaySoapClient::timeout()`; environmental (server `max_execution_time`).
- **Connection-probe test coverage gaps** [components/gateways/nonmerchant/kuickpay/tests/KuickPayConnectionProbeTest.php] — the `connection.unavailable` (curl-missing) branch, the redirect/404-as-success path, and the validation-fails-with-sentinel skip are untested; the reflection seam stubs `Language::_`, so a missing/misspelled language key would not fail the test. Reason: optional hardening; all four `!error.connection.*` keys verified present today.
- **Story Task 6.4 cites broken runner `-c build/phpunit.xml`** [_bmad-output/kuickpay/implementation-artifacts/1-4-run-safe-kuickpay-connection-tests.md] — PHPUnit resolves `tests/bootstrap.php` relative to `build/` and looks for a missing `build/tests/bootstrap.php`. Reason: pre-existing and documented in project-context.md:72; use `--bootstrap tests/bootstrap.php tests` (green: 28 tests / 136 assertions).

## Deferred from: code review of 2-1-create-durable-customer-voucher-records (2026-06-10)

- **Deterministic reference collides with a non-pending voucher → permanent `not_ready` — RESOLVED 2026-06-10 by Story 2.2.** [plugins/kuickpay_reconcile/lib/KuickPayVoucherReferenceService.php] Story 2.2 replaced the fixed `'0000'` deterministic prefix with a secure random 4-digit prefix plus bounded company-scoped registration/consumer uniqueness pre-checks. A prior non-pending voucher for the same invoice no longer forces the same reference value forever; random regeneration can produce a fresh value, and repeated reference-value collisions fail closed with `uniqueness_exhausted`.
- **Duplicate invoice IDs in `invoice_amounts` abort the whole create** [plugins/kuickpay_reconcile/lib/KuickPayVoucherReferenceService.php:72] — the invoice-link loop does not dedupe; a repeated invoice id violates the `(voucher_id, invoice_id)` unique key, rolls back the voucher, and returns null → silent `not_ready`. Reason: the normal Blesta pay flow passes distinct invoices, and the failure is fail-closed (no bad write); multi-invoice handling is owned by Story 2.4. (Edge Case Hunter, MEDIUM.)
- **Reuse lookup keyed only on the first invoice** [plugins/kuickpay_reconcile/lib/KuickPayVoucherReferenceService.php:42] — `getOrCreateForInvoiceContext` reuses by `invoice_amounts[0]['id']` only, so a multi-invoice payment with a partially overlapping set can reuse/display a voucher whose linked invoices and amount differ from what is being paid. Reason: matches spec Task 3.2 (reuse by first invoice); multi-invoice/active-context correctness is Story 2.4. (Edge Case Hunter + Acceptance Auditor + Blind Hunter, MEDIUM.)
- **No minimum/positive amount check** [plugins/kuickpay_reconcile/models/kuickpay_vouchers.php:225] — the amount rule only checks non-empty + decimal format, so `0` / `0.00` passes and creates a zero-value pending voucher shown to the customer as payable. Reason: amount-change/value gating is Story 2.4; AC1 specifies no minimum. (Edge Case Hunter, LOW.)
- **`KuickpayVouchers::edit()` not company-scoped + dead empty-fields guard** [plugins/kuickpay_reconcile/models/kuickpay_vouchers.php:80] — the UPDATE filters on `id` only (other read methods scope by `company_id`), and `if (empty($fields))` can never be true because `date_updated` is always added. Reason: `edit()` has no caller in 2.1; its signature matches the spec; tenant-scoped edit + guard cleanup are future hardening for the story that first mutates vouchers. (Blind Hunter, LOW.)

## Deferred from: code review of 2-2-generate-registration-number-and-consumer-number (2026-06-10)

- **`expandPattern()` length check counts bytes, not characters** [plugins/kuickpay_reconcile/lib/KuickPayVoucherReferenceService.php:233] — `strlen($expanded) > 64` measures bytes while the `registration_number`/`consumer_number` columns are `varchar(64)` (characters); a multibyte literal pattern or multibyte `institution_id` near the limit would be rejected at ~16–32 chars. Reason: pre-existing from 2.1's identical `strlen` guard; unreachable via the ASCII-enforced pattern charset (`/^[A-Za-z0-9_{}+\-]+$/`) and the numeric institution/invoice/random inputs; fails closed (never corrupts) if ever hit. Switch to `mb_strlen` if a non-ASCII reference component is ever introduced. (Edge Case Hunter, LOW.)
- **`company_id` lookup vs insert value mismatch on a null-company context** [plugins/kuickpay_reconcile/lib/KuickPayVoucherReferenceService.php:59,90] — collision lookups use `(int) ($context['company_id'] ?? 0)` (scope `0`) while the insert persists `$context['company_id'] ?? null`; on a null company id the uniqueness pre-check queries a different scope than the row would write under, then the model rejects the null and `create()` returns null (benign `not_ready`, `lastError` null). Reason: `Configure::get('Blesta.company_id')` is always a valid int in an authenticated HTTP pay flow, so this cannot occur in practice; fails closed with no bad write. Normalize `company_id` once for both lookup and insert if a non-HTTP entry path is ever added. (Edge Case Hunter, LOW.)
- **Throwable-catch and benign null paths record no admin diagnostic** [plugins/kuickpay_reconcile/lib/KuickPayVoucherReferenceService.php:119] — failures routed through the `Throwable` catch, the empty-`invoice_id` early return, and the `create()`-null race-recovery path return null with `lastError` null, so `recordReferenceGenerationFailure()` logs nothing — an admin sees a missing voucher with zero trace for a swallowed DB exception. Reason: **by design** — spec Task 2.4/2.5 explicitly forbids categorizing the catch/benign paths (they are transient `not_ready`, not AC2 generation failures); a durable `voucher.generation_failed` audit event in `kuickpay_audit_events` is owned by Epic 4 / Story 4.5. (Edge Case Hunter, MEDIUM.)
- **4-digit random prefix gives a 10k keyspace** [plugins/kuickpay_reconcile/lib/KuickPayVoucherReferenceService.php:141] — `randomInt()` draws from `random_int(0, 9999)`, so per-invoice the only entropy is 4 digits; under high invoice volume sharing a company scope, regenerate-on-collision retries could become more frequent and a pathological run could exhaust `MAX_REFERENCE_ATTEMPTS`. Reason: informational — reference-value uniqueness still derives primarily from the appended `invoice_id`, the bounded retry fails closed at `uniqueness_exhausted` (no corruption, no duplicate active reference), and the 4-digit width is fixed by the confirmed KuickPay contract. Revisit only if production telemetry shows elevated exhaustion. (Blind Hunter, LOW.)

## Deferred from: code review of 3-3-reconcile-pending-vouchers-by-single-inquiry (2026-06-10)

- **`getResumeCursor` resumes only `trigger_type='cron'` + `status='aborted'` runs, but `run()` calls it for every trigger** [plugins/kuickpay_reconcile/models/kuickpay_reconciliation_runs.php:42-54] — the resume query hard-filters `trigger_type='cron'`, so a future `manual`/bulk caller of the shared service would inherit the last aborted *cron* run's cursor and start mid-set rather than at 0, silently skipping low-id eligible vouchers for that run. Reason: not reachable in this story (cron is the only caller); the service is explicitly the shared entry for Story 3.7 (bulk) and Epic 4 ("Check Now"). Scope `getResumeCursor` by the current run's `trigger_type` (or pass it in) when the first manual/bulk caller lands. (Acceptance Auditor + Blind Hunter, LOW/latent.)
- **No DB transaction wraps the per-voucher writes (voucher edit + item record + audit)** [plugins/kuickpay_reconcile/lib/KuickPayReconcileService.php:114-152] — `persistEvidence()` (voucher edit), `itemRepository->record()`, and `recordEvidenceAudit()` are three independent writes; a crash between them leaves the voucher mutated with a missing item/audit trail. The run cursor is also advanced (`updateCursor`) before processing, so an aborted in-flight voucher is marked done. Reason: impact is an incomplete audit trail, not corruption — voucher state self-heals on the next run (failed runs restart at cursor 0); project-context calls for transactions on multi-write ops, so wrap the per-voucher writes in `begin()/commit()/rollBack()` as a hardening pass. (Blind Hunter + Edge Case Hunter, LOW-MEDIUM.)
- **AC13 enablement-gate skip path has no automated test** [plugins/kuickpay_reconcile/lib/KuickPayReconcileService.php:267-295] — every test injects `gateway_config`, so `run()` short-circuits before `gatewayConfigForCompany()` and the entire AC13 skip behavior (gateway not installed / not PKR-associated / `reconciliation_enabled` not truthy) is never exercised; AC14 ("honest, fixture-backed verification") is therefore unmet for AC13. Reason: the production gate code was read and verified correct during review; clean coverage requires a `GatewayManager` stub / testability seam (`getInstalledNonmerchant` is loaded via `Loader::loadModels`), which is more than a mechanical add. Add the skip-path test when the harness grows a GatewayManager double. (Acceptance Auditor, LOW.)
- **Per-voucher processing exception records an item row but emits no audit event** [plugins/kuickpay_reconcile/lib/KuickPayReconcileService.php:138-149] — the `catch (Throwable)` path writes a `kuickpay_reconciliation_items` row with `error_class='reconcile_exception'` but does not call `KuickPayAuditService::record(...)`, so a voucher that throws during processing has an item row with no corresponding audit event. Reason: partial AC9 gap — transport timeouts (the common failure) return `retry` evidence and ARE audited via `evidence.retry_decision`; only genuine exceptions are un-audited, and the item row still captures them. A redacted `evidence.error` event is a small add best batched with the Story 4.5 audit-viewing surface. (Acceptance Auditor, LOW.)
- **`insertLock()` swallows all exceptions as "lock held"** [plugins/kuickpay_reconcile/models/kuickpay_reconcile_locks.php:10-26] — `catch (Exception) { return false; }` treats a genuine DB/infra failure (connection loss, schema error) identically to a duplicate-key collision; the caller then falls through to stale-reclaim and, on failure, skips the run with `reason=lock_held` and no surfaced error. Reason: fail-safe (skipping reconciliation never corrupts state), but it hides real infrastructure failures from operators; distinguish duplicate-key from other SQL errors and surface/audit the latter as a hardening pass. (Blind Hunter + Edge Case Hunter, LOW.)
- **`gatewayConfigForCompany()` defaults missing meta keys to `''`** [plugins/kuickpay_reconcile/lib/KuickPayReconcileService.php:284-294] — every credential/config field falls back to `?? ''`, so if `wsdl_url` / `inquiry_username` / `inquiry_password` are absent the service still constructs a SOAP client with empty credentials and runs the whole batch, failing every inquiry and pushing vouchers toward `manual_review` via burned retry counters instead of cleanly skipping. Reason: low risk in practice — these are required gateway settings validated at save time (Epic 1), and `reconciliation_enabled` already gates the run; add an explicit "required keys present" guard that skips with a config error if hardening is wanted. (Blind Hunter, LOW.)

## Deferred from: code review of 2-3-map-invoice-data-and-issue-kuickpay-voucher (2026-06-10)

- **Concurrent double-submit can clobber an issued reference** [plugins/kuickpay_reconcile/lib/KuickPayIssuanceService.php] — two near-simultaneous requests (double-click / second tab) can both pass the `status==='pending' && empty(kuickpay_reference)` guard and the Guard-B reload reads before either persists; one gets `00`+reference while the slower one gets `94`/duplicate → `manual_review` and last-writer-wins overwrites the row to `manual_review` with a null reference, hiding a successfully-issued payable voucher. Reason: **explicitly out of 2.3 scope** — Dev Notes "CONCURRENCY RESIDUAL" assigns the schema-level active-context uniqueness that closes this to Story 2.4 (architecture.md:351; residual accepted at 2.2). Story 2.3's contract is sequential-reload safety only, which holds. (Edge Case Hunter, HIGH but accepted deferral.) **CLOSED at the schema layer (Story 5.2, 2026-06-13):** the `uniq_kuickpay_vouchers_active_context` unique key makes two concurrent same-invoice-set pending inserts impossible — the loser's INSERT fails and the create-null fall-through returns the winner (proven against the real DB).
- **Reload `display` shows the stored voucher amount without re-checking the current invoice balance** [components/gateways/nonmerchant/kuickpay/kuickpay.php — `buildProcess()` display branch + `getLatestByInvoiceId`] — once a `pending`+reference voucher exists, a reload displays its stored amount; if the invoice amount changed (partial payment, credit, line-item edit) the customer is shown a reference for the old amount. Reason: a voucher is issued for a fixed amount and amount-change/active-context handling is Story 2.4 / reconciliation territory; the mismatch fails closed at reconcile time (routes to manual review) rather than mispaying. (Edge Case Hunter, MEDIUM.)

## Deferred from: code review of 3-4-validate-confirmed-payment-evidence (2026-06-10)

- **Confirmed evidence with a null/malformed paid date passes the gate** [components/gateways/nonmerchant/kuickpay/lib/KuickPayResponseParser.php:504; plugins/kuickpay_reconcile/lib/KuickPayReconcileService.php:220] — an inquiry reaches `STATUS_CONFIRMED_UNPOSTED` without the parser validating `fields[2]` (Transaction_Date); `normalizeDate` simply nulls a malformed/empty date and the validator never checks `paidAt()`, so a confirmed voucher can persist with `date_paid = NULL`. Reason: paid date is not in the AC3–AC7 check-list and the root allowance lives in the 3.2 parser; impact is nil in 3.4 (nothing posts) and the probability is low (a genuinely-paid bill carries a date). **Must be resolved before Story 3.5 posting** — a Blesta transaction needs a paid date; enforce date presence on the confirmed path (parser or validator) there. (Edge Case Hunter, HIGH for 3.5 / LOW for 3.4.)
- **Same invoice can be allocated by two distinct *pending* vouchers** [plugins/kuickpay_reconcile/lib/KuickPayEvidenceValidator.php — voucherIsFresh/invoiceMatches] — `findActiveByInvoiceId` only matches `confirmed_unposted`/`posted` siblings, and `invoiceMatches` compares the full invoice `due` (not reduced by in-flight unposted links), so two concurrent pending vouchers on one invoice can both pass the gate. Reason: no money moves in 3.4; the real guard is Story 3.5 row-locked posting + final re-validation + idempotency, which is exactly where double-allocation must be blocked. Re-validate sibling/`due` coverage under the posting row lock in 3.5. (Edge Case Hunter, MEDIUM, deferred to 3.5.) **CLOSED at the schema layer for the exact-invoice-set case (Story 5.2, 2026-06-13):** two distinct *pending* vouchers on the same company-scoped invoice set are now impossible at the database layer (`uniq_kuickpay_vouchers_active_context`), independent of the 3.5 posting row lock.
- **Consumer-number format consistency on the bulk path (3.7)** [plugins/kuickpay_reconcile/lib/KuickPayEvidenceValidator.php — referenceMatches] — voucher `consumer_number` stores an `INSTITUTION_ID`-prefixed value; when bulk evidence carries a non-null `consumer_number`, the strict equality could route a legitimately-paid voucher to `manual_review`. Reason: correct and skipped for 3.4 (single-inquiry evidence always has a null consumer number, per the single-identity contract); the column match only becomes meaningful when Story 3.7 consumes `BillPaymentBulkInquiry` evidence. Confirm the bulk consumer-number format matches the stored value when 3.7 lands. (Blind Hunter, forward-looking watch-item.)
- **AC10 PHP 8.2 verification gate outstanding** [plugins/kuickpay_reconcile/*] — only PHP 8.3.31 and 7.4.33 are present on this host (no 8.2 binary), so the suite ran under 8.3. Reason: the changed code is 8.2-syntax-compatible (typed properties, nullable returns, `array_pad`/`str_pad`/`preg_match` — no 8.3+ syntax; audited and confirmed) and the suite is green (34 tests / 109 assertions), but the story's own AC10 requires a confirmation run under PHP 8.2 before merge. Pre-merge action, not a code defect. (Acceptance Auditor, informational.)

## Deferred from: code review of 2-4-gate-changed-amounts-and-multi-invoice-attempts (2026-06-10)

- **`retireVoucher()` reports success on a no-op cancel and is non-atomic with its audit** [plugins/kuickpay_reconcile/lib/KuickPayVoucherReferenceService.php:227] — `repository->edit()` returns void with no affected-row check, so a cancel that matches nothing (wrong/zero `company_id`, already-`cancelled` row) still returns `true` and records `voucher.replaced`; and a `record()` throw after a successful `edit()` is caught as `retireVoucher()===false`, conflating audit failure with retire failure. Reason: the non-atomic edit-then-audit ordering is mandated by the spec (durable edit first, no cross-model transaction) and the path **self-heals** on the next reload (a `cancelled` latest → ALLOW → fresh create); critically, `replace` is code-gated out of production until Stories 3.4/3.5/3.7 land, so this state is unreachable now. Harden `edit()` to surface affected-row count (skip the audit + return `false` on a no-op) when un-gating `replace`. (Blind Hunter + Edge Case Hunter, MEDIUM/latent.)
- **Replace-path non-amount failures surface the `amount_changed` notice** [components/gateways/nonmerchant/kuickpay/kuickpay.php:923] — after a successful retire, if `getOrCreateForInvoiceContext()`+issuance returns null for a reason other than amount change (uniqueness exhausted, transport error), `displayVoucherForContext()` hard-codes `process_notice => 'amount_changed'`. Reason: fails safe (no payable shown) and `replace` is production-gated off; map the notice via `getLastError()` when un-gating. (Edge Case Hunter, LOW/latent.)
- ✅ **CLOSED by Story 5.4** — the genuine create fall-through now sets `lastError = 'create_failed'` and emits a durable `voucher.generation_failed` audit (reason `create_failed`); not set on the race-recovery return or outer catch. _Original:_ **Create-failure fall-through sets no `lastError`** [plugins/kuickpay_reconcile/lib/KuickPayVoucherReferenceService.php:174] — the final `return null` after a failed create + empty re-lookup leaves `lastError` unset, so `recordReferenceGenerationFailure()` logs a generic (null) reason with no operator breadcrumb. Reason: pre-existing (predates 2.4); the new 2.4 null paths (`amount_changed`, `duplicate_invoice_id`) do set `lastError`. Set a `create_failed` code on the fall-through as a diagnostic-quality pass. (Edge Case Hunter, LOW.)
- **`normalizeAmount()` truncates beyond 2 decimals and passes invalid input through raw** [components/gateways/nonmerchant/kuickpay/kuickpay.php + plugins/kuickpay_reconcile/lib/KuickPayVoucherReferenceService.php, both copies] — `substr(...,0,2)` drops the 3rd+ decimal without rounding (`100.999`→`100.99`), and a non-numeric/negative amount returns the trimmed raw string rather than failing closed. Reason: self-consistent (both compare sides run through the same function) and Blesta supplies 2-dp amounts, so neither is reachable in practice; round (bcmath/`number_format`) and reject invalid input if the contract is ever exercised with sub-cent or malformed values. (Edge Case Hunter + Blind Hunter, LOW/latent.)
- **Gateway single-invoice reload regression guard is fake-driven** [components/gateways/nonmerchant/kuickpay/tests/KuickPayVoucherGatewayHelpersTest.php:685] — `testDisplayVoucherForContextShowsMatchingIssuedVoucher` injects a fake `requestMatchesVoucher` that returns a stubbed `true`, so the real empty-links amount-only compare is not exercised at the gateway layer. Reason: the underlying behavior IS covered by the service-layer `testRequestMatchesVoucherTreatsUnloadedLinksAsAmountOnly`; add an end-to-end gateway assertion using the real comparator if higher fidelity is wanted. (Acceptance Auditor, LOW.)

## Deferred from: code review of 3-5-post-safe-blesta-transactions (2026-06-10)

- **Head-of-line blocking in the bounded posting batch** [plugins/kuickpay_reconcile/lib/KuickPayPostingService.php:54] — `postConfirmed()` fetches one ascending-by-id page and the `failed` outcome (transaction `add()`/`apply()` error) leaves the voucher `confirmed_unposted`, so a deterministically-failing low-id voucher re-occupies the front of every batch; with >100 stuck rows, newer postable vouchers are never reached. Reason: per AC6 the `failed` outcome intentionally preserves `confirmed_unposted` for retry, and there is no posting-side retry counter — a backoff/escalation-to-`manual_review`-after-N-attempts policy is Epic-4 scope. Add a retry cap or skip-cursor for repeatedly-failing rows when retry policy lands. (Edge Case Hunter, LOW.)
- **`getByTransactionId` inspects a single arbitrary row when duplicate references exist** [plugins/kuickpay_reconcile/lib/KuickPayPostingService.php:136] — `transactions.transaction_id` has no unique constraint, and `getByTransactionId()` does a single `fetch()` with no ordering; if two approved transactions share the reference for the same client+gateway, adoption verifies the wrong row (or creates a new one beside an existing applied transaction). Reason: pre-existing core `Transactions` behavior, low likelihood because `kuickpay_reference` is per-voucher unique and only arises from external/manual duplication; the voucher row lock serializes same-voucher posting. Consider selecting the most-recent approved+applied match if a uniqueness guarantee is ever relaxed. (Edge Case Hunter, LOW.)

## Deferred from: code review of 3-6-handle-expiry-late-partial-and-overpayment-cases (2026-06-10)

- **Story `baseline_commit` frontmatter is stale** [_bmad-output/kuickpay/implementation-artifacts/3-6-handle-expiry-late-partial-and-overpayment-cases.md:2] — `baseline_commit: d8b391a1` points to the "2.6 create story" commit, which predates Story 2-6's implementation, so a literal `baseline..HEAD` diff pulls in already-reviewed 2-6 work (`process.pdt`, payment-instruction rendering) and the epic-2 retro. This review was scoped instead to `7db4dc14..HEAD`, which matches the 3-6 File List exactly. Planning-artifact hygiene only — no shipped-code impact. Recommend updating the frontmatter to the correct 3-6 base commit so future automated reviews scope correctly. (Reviewer, informational.)

## Deferred from: code review of 3-7-run-bulk-reconciliation-safety-net (2026-06-11)

- **Bulk run-summary counts overlap and `total_eligible` counts returned rows, not eligible vouchers** [plugins/kuickpay_reconcile/lib/KuickPayReconcileService.php:212, :224-228] — an unmatched bulk row increments `total_checked`, `total_manual_review` AND `total_unmatched`, so the same row appears under two disjoint-looking headings in the result message; and `total_eligible` is set to the count of all returned rows (including unmatched/foreign Consumer Numbers), unlike the single path where it means eligible local vouchers. Reason: reporting-semantics only, no payment-safety impact, and the polished run-summary view is Epic 4 / Story 4.4 (FR24–FR27) which owns the final count model and display. Decide the partitioning (unmatched as a subset of manual-review vs disjoint) when building the run-detail view. (Blind Hunter + Edge Case Hunter + Acceptance Auditor, LOW.)
- **Bulk `run_date` has no upper bound** [plugins/kuickpay_reconcile/controllers/admin_main.php:99] — `validRunDate` only checks `YYYY-MM-DD` format + round-trip, so a format-valid future or implausibly-old date is accepted and sent to the provider (best case a confusing empty completed run). Reason: minimal-trigger scope only; the full date-input form with bounds/labels is Epic 4 / Story 4.4 per the Scope Decision. Bound the date to ≤ today (and an optional max look-back) when the real form lands. (Edge Case Hunter, LOW.)


## Deferred from: code review of 4-1-search-and-filter-kuickpay-vouchers.md (2026-06-12)

- Admin permission is not explicitly enforced in the controller (`controllers/admin_vouchers.php:46`) — deferred, pre-existing; `admin_main` uses the same pattern and framework route-level enforcement is assumed.

## Deferred from: story-context validation of 4-3-run-safe-manual-voucher-actions (2026-06-12)

- **`persistEvidence`'s terminal write is not status-guarded** [plugins/kuickpay_reconcile/lib/KuickPayReconcileService.php:435] — `persistEvidence()` ends in an unguarded `voucherRepository->edit($id, $company_id, $vars)` with no `WHERE status IN (...)` clause. Under cron-only operation the `reconcile_pending` batch lock serializes writers so this never bites. **Story 4.3's manual Check Now deliberately skips that lock**, which exposes the ungated write to a second concurrent writer: if the cron confirms a voucher (`pending → confirmed_unposted`) *while* a manual reconcile's SOAP inquiry is in flight, the manual call's stale-guard (`:403-404`) sets `manual_review` and `:435` overwrites the cron's `confirmed_unposted`, demoting a just-confirmed (paid) voucher to a stuck `manual_review` with a dangling `date_paid`. Reason: **safe** (fails closed, evidence preserved, NFR9 honored) but an operational regression; the fix is shared Epic-3 reconcile-service code, out of 4.3 scope. 4.3 mitigates the common case with a pre-inquiry fresh-status short-circuit and accepts the during-inquiry interleave as a documented residual. Recommend status-guarding the `:435` UPDATE (`WHERE status IN ('pending','retry')`) so a racing manual reconcile matches 0 rows instead of clobbering. (Story-context validation, round 2 — safety lens, LOW/operational.)

## Deferred from: code review of 4-5-record-structured-logs-and-audit-events (2026-06-12)

- ✅ **CLOSED** — runtime closed by Story 5.3 (`01682753`: bulk catch writes the item row + best-effort `evidence.error` via `recordProcessVoucherFailure`); Story 5.4 ratified the bulk/single symmetry + idempotency with dedicated regression tests. _Original:_ **Bulk per-voucher exception emits no `evidence.error` audit event** [plugins/kuickpay_reconcile/lib/KuickPayReconcileService.php:350] — Story 4.5 adds `evidence.error` (AC4/Task 6) only in `processVoucher()` (cron-single + admin Check Now). The bulk loop's own per-voucher `catch (Throwable)` at `:350-353` sets `$error=true` and counts the failure but emits no `evidence.error` and writes no `kuickpay_reconciliation_items` row, so a genuine per-voucher exception during a bulk run leaves no durable audit trace. Reason: Task 6 deliberately scopes `evidence.error` to the `processVoucher()` catch ("the catch that today writes only a kuickpay_reconciliation_items row"); the bulk catch is structurally different and pre-dates 4.5, so closing it would expand beyond 4.5's defined scope. Recommend a follow-up emitting a best-effort `evidence.error` (+ item row) in the bulk catch for symmetric lifecycle coverage. (Edge Case Hunter, LOW.)
- ✅ **CLOSED by Story 5.4** — `normalizeContextInvoiceAmounts()` now records the conflicting id on a private `$conflictInvoiceId` member (validator signature unchanged); the emit names it, falling back to `firstContextInvoiceId()`. _Original:_ **`voucher.generation_failed` can mislabel the offending `invoice_id` on the `duplicate_invoice_id` path** [plugins/kuickpay_reconcile/lib/KuickPayVoucherReferenceService.php:290] — the call-site emit (`:67-72`) uses `firstContextInvoiceId()`, which returns the FIRST context row's id (or `0` if that row lacks an id), not the invoice whose duplicate-with-differing-amount actually triggered the failure in `normalizeContextInvoiceAmounts()` (`:443`). For a multi-invoice context where the conflicting pair is not first, the audit payload's `invoice_id` names the wrong invoice. Reason: diagnostic-only field (the `reason` token is correct), Task 5 emits at the call site and explicitly forbids changing the pure validator's signature, and the path requires `multi_invoice_policy === 'allow'` plus an internal id collision (rare). Expose the conflicting id from the validator via a private member (not its signature) if precise attribution is ever wanted. (Blind Hunter + Edge Case Hunter, LOW.)
