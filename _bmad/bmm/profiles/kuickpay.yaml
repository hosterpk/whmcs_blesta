# BMM profile — KuickPay (payment gateway initiative).
#
# Activate it for THIS working copy with:  ./bmad-use kuickpay
# (that copies this file to the local, gitignored _bmad/bmm/config.user.yaml).
#
# Tracked template. The live config.user.yaml is per-checkout and NOT tracked,
# so different working copies / worktrees can sit on different profiles at once.

planning_artifacts:       "{project-root}/_bmad-output/kuickpay/planning-artifacts"
implementation_artifacts: "{project-root}/_bmad-output/kuickpay/implementation-artifacts"
