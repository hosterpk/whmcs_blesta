<?php /** @var View $this **/ ?>

<?php
$this->Widget->clear();
$this->Widget->setLinkButtons([
    [
        'name' => $this->_('DirectAdmin.back_to_manage', true),
        'icon' => 'bi bi-arrow-left me-1',
        'attributes' => ['href' => $this->base_uri . 'settings/company/modules/manage/' . $module->id],
    ],
]);
$this->Widget->create($this->_('DirectAdmin.add_row.box_title', true));
?>
        <?php
        $this->Form->create(null, ['id' => 'add_row', 'class' => 'disable-on-submit']);
        $this->Form->fieldHidden('account_count', isset($vars->account_count) ? $vars->account_count : 0);
        ?>
    <h6 class="form-section-header"><?php $this->_('DirectAdmin.add_row.basic_title'); ?></h6>

    <div class="mb-3">
        <label for="server_name" class="form-label">
            <?php $this->_('DirectAdmin.row_meta.server_name'); ?>
        </label>
        <?php $this->Form->fieldText('server_name', isset($vars->server_name) ? $vars->server_name : null, [
            'id' => 'server_name',
            'class' => 'form-control',
        ]); ?>
    </div>

    <div class="mb-3">
        <label for="host_name" class="form-label">
            <?php $this->_('DirectAdmin.row_meta.host_name'); ?>
        </label>
        <?php $this->Form->fieldText('host_name', isset($vars->host_name) ? $vars->host_name : null, [
            'id' => 'host_name',
            'class' => 'form-control',
        ]); ?>
    </div>

    <div class="mb-3">
        <label for="port" class="form-label">
            <?php $this->_('DirectAdmin.row_meta.port'); ?>
        </label>
        <?php $this->Form->fieldText(
            'port',
            isset($vars->port) ? $vars->port : $this->_('DirectAdmin.row_meta.default_port', true),
            ['id' => 'port', 'class' => 'form-control'],
        ); ?>
    </div>

    <div class="mb-3">
        <label for="user_name" class="form-label">
            <?php $this->_('DirectAdmin.row_meta.user_name'); ?>
        </label>
        <?php $this->Form->fieldText('user_name', isset($vars->user_name) ? $vars->user_name : null, [
            'id' => 'user_name',
            'class' => 'form-control',
        ]); ?>
    </div>

    <div class="mb-3">
        <label for="password" class="form-label">
            <?php $this->_('DirectAdmin.row_meta.password'); ?>
        </label>
        <?php $this->Form->fieldText('password', isset($vars->password) ? $vars->password : null, [
            'id' => 'password',
            'class' => 'form-control',
        ]); ?>
    </div>

    <div class="mb-3">
        <label for="http_version" class="form-label">
            <?php $this->_('DirectAdmin.row_meta.http_version'); ?>
        </label>
        <?php $this->Form->fieldSelect(
            'http_version',
            isset($http_versions) ? $http_versions : null,
            isset($vars->http_version) ? $vars->http_version : null,
            ['id' => 'http_version', 'class' => 'form-select'],
        ); ?>
    </div>

    <div class="mb-3">
        <div class="form-check">
            <?php $this->Form->fieldCheckbox(
                'use_ssl',
                'true',
                (isset($vars->use_ssl) ? $vars->use_ssl : 'true') == 'true',
                ['id' => 'use_ssl', 'class' => 'form-check-input'],
            ); ?>
            <label for="use_ssl" class="form-check-label">
                <?php $this->_('DirectAdmin.row_meta.use_ssl'); ?>
            </label>
        </div>
    </div>

    <div class="mb-3">
        <label for="ip_address" class="form-label">
            <?php $this->_('DirectAdmin.row_meta.ip_address'); ?>
        </label>
        <?php $this->Form->fieldSelect(
            'ip_address',
            isset($ip_addresses) ? $ip_addresses : null,
            isset($vars->ip_address) ? $vars->ip_address : null,
            ['id' => 'ip_address', 'class' => 'form-select'],
        ); ?>
    </div>

    <div class="mb-3">
        <label for="account_limit" class="form-label">
            <?php $this->_('DirectAdmin.row_meta.account_limit'); ?>
        </label>
        <?php $this->Form->fieldText('account_limit', isset($vars->account_limit) ? $vars->account_limit : null, [
            'id' => 'account_limit',
            'class' => 'form-control',
        ]); ?>
    </div>

    <h6 class="form-section-header mt-4"><?php $this->_('DirectAdmin.add_row.name_servers_title'); ?></h6>

    <div class="mb-3">
        <table class="table">
            <thead>
                <tr>
                    <th><?php $this->_('DirectAdmin.add_row.name_server_col'); ?></th>
                    <th><?php $this->_('DirectAdmin.add_row.name_server_host_col'); ?></th>
                    <th style="width: 50px;"></th>
                </tr>
            </thead>
            <tbody>
                <?php
                $num_servers = count(isset($vars->name_servers) ? $vars->name_servers : []);
                for ($i = 0; $i < max(2, $num_servers); $i++) { ?>
                <tr class="ns_row">
                    <td><?php $this->_(
                        'DirectAdmin.row_meta.name_server',
                        false,
                        '<span>' . ($i + 1) . '</span>',
                    ); ?></td>
                    <td><?php $this->Form->fieldText(
                        'name_servers[]',
                        isset($vars->name_servers[$i]) ? $vars->name_servers[$i] : null,
                        ['class' => 'form-control'],
                    ); ?></td>
                    <td class="text-center">
                        <button type="button" class="btn btn-sm btn-outline-danger ns_row_remove" title="<?php $this->_('DirectAdmin.add_row.remove_name_server'); ?>">
                            <i class="bi bi-trash"></i>
                        </button>
                    </td>
                </tr>
                        <?php }
                ?>
            </tbody>
        </table>
    </div>
    <a class="btn btn-outline-success btn-sm ns_row_add" href="#"><i class="bi bi-plus-lg"></i> <span><?php $this->_(
        'DirectAdmin.add_row.name_server_btn',
    ); ?></span></a>

    <h6 class="form-section-header mt-4"><?php $this->_('DirectAdmin.add_row.notes_title'); ?></h6>

    <div class="mb-3">
        <?php $this->Form->fieldTextarea('notes', isset($vars->notes) ? $vars->notes : null, [
            'id' => 'notes',
            'class' => 'form-control',
        ]); ?>
    </div>

    <?php $this->Widget->footer(); ?>
    <div class="d-flex justify-content-end">
        <?php $this->Form->fieldSubmit('save', $this->_('DirectAdmin.add_row.add_btn', true), [
            'class' => 'btn btn-primary',
        ]); ?>
    </div>

                        <?php $this->Form->end(); ?>
                        <?php $this->Widget->end(); ?>

<script type="text/javascript">
    document.addEventListener('DOMContentLoaded', function() {
        // Add a row
        document.querySelector('.ns_row_add').addEventListener('click', function(e) {
            e.preventDefault();
            var fields = document.querySelector('tr.ns_row').cloneNode(true);
            fields.querySelectorAll('input').forEach(function(el) {
                el.value = '';
            });
            fields.querySelector('td:first-child span').textContent = document.querySelectorAll('tr.ns_row').length + 1;
            var ns_rows = document.querySelectorAll('tr.ns_row');
            ns_rows[ns_rows.length - 1].insertAdjacentElement('afterend', fields);
        });
        // Remove a row - event delegation for dynamic rows
        document.querySelector('table tbody').addEventListener('click', function(e) {
            var removeLink = e.target.closest('.ns_row_remove');
            if (removeLink) {
                if (document.querySelectorAll('tr.ns_row').length > 1) {
                    removeLink.closest('tr').remove();
                    // Reorder the counts for these rows
                    var i = 1;
                    document.querySelectorAll('tr.ns_row').forEach(function(row) {
                        row.querySelector('td:first-child span').textContent = i++;
                    });
                }
                e.preventDefault();
            }
        });
        // Update IP addresses
        updateIpAddreses();
        document.querySelectorAll('#add_row input, #add_row select').forEach(function(el) {
            el.addEventListener('change', updateIpAddreses);
        });
    });

    // Update IP addresses
    function updateIpAddreses() {
        var form = document.getElementById('add_row');
        var params = new FormData(form);
        blestaRequest('GET', location.pathname + '?fetch_ips=true', params, function(data) {
                var select = document.getElementById('ip_address');
                select.innerHTML = '';
                data.forEach(function(value) {
                    var option = document.createElement('option');
                    option.value = value;
                    option.textContent = value;
                    select.appendChild(option);
                });
                var selectedValue = '<?php echo $this->Html->safe($vars->ip_address ?? null); ?>';
                if (selectedValue) {
                    var options = select.querySelectorAll('option');
                    options.forEach(function(opt) {
                        if (opt.textContent === selectedValue) {
                            opt.selected = true;
                        }
                    });
                }
            },
            null, {
                dataType: 'json'
            });
    }
</script>
