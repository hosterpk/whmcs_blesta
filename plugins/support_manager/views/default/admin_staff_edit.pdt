
                <?php
                echo (isset($message) ? $message : null);

                $this->Widget->clear();
                $this->Widget->setStyleSheet($this->view_dir . 'css/support.css', ['id' => 'support_manager_styles']);
                $this->Widget->create($this->_('AdminStaff.edit.boxtitle_editstaff', true, (isset($staff->first_name) ? $staff->first_name : null), (isset($staff->last_name) ? $staff->last_name : null)));
                ?>
                    <?php $this->Form->create(null, ['id' => 'staff', 'class' => 'disable-on-submit']);?>
                        <h6 class="form-section-header"><?php $this->_('AdminStaff.edit.heading_schedule');?></h6>
                        <div class="table-responsive">
                        <table class="table table-hover mb-0">
                            <thead>
                            <tr>
                                <th><?php $this->_('AdminStaff.edit.heading_day');?></th>
                                <th>
                                    <?php $this->_('AdminStaff.edit.heading_all_day');?>
                                    <i class="bi bi-info-circle ms-1" data-bs-toggle="tooltip" data-bs-placement="top" title="<?php echo $this->Html->safe($this->_('AdminStaff.!tooltip.all_day', true));?>"></i>
                                </th>
                                <th><?php $this->_('AdminStaff.edit.heading_start_time');?></th>
                                <th><?php $this->_('AdminStaff.edit.heading_end_time');?></th>
                            </tr>
                            </thead>
                            <tbody>
                            <?php
                            $i = 0;
                            foreach ((isset($days) ? $days : []) as $key => $day) {
                            ?>
                            <tr<?php echo ($i%2 == 1) ? ' class="odd_row"' : '';?>>
                                <td>
                                    <?php
                                    echo (isset($day) ? $day : null);
                                    $this->Form->fieldHidden('schedules[' . $i . '][day]', $key);
                                    ?>
                                </td>
                                <td>
                                    <?php
                                    // Set whether the box should be checked
                                    $all_day_checked = false;
                                    $start_time = (isset($vars->schedules[$i]['start_time']) ? $vars->schedules[$i]['start_time'] : null);
                                    $end_time = (isset($vars->schedules[$i]['end_time']) ? $vars->schedules[$i]['end_time'] : null);
                                    if (((isset($vars->schedules[$i]['all_day']) ? $vars->schedules[$i]['all_day'] : false) == 1) || (!empty($start_time) && $start_time == $end_time)) {
                                        $all_day_checked = true;
                                    }

                                    $this->Form->fieldCheckbox('schedules[' . $i . '][all_day]', '1', $all_day_checked, ['class' => 'form-check-input all_day']);
                                    ?>
                                </td>
                                <td><?php $this->Form->fieldSelect('schedules[' . $i . '][start_time]', (isset($times) ? $times : null), (isset($vars->schedules[$i]['start_time']) ? $vars->schedules[$i]['start_time'] : null), ['class' => 'form-select time']);?></td>
                                <td><?php $this->Form->fieldSelect('schedules[' . $i . '][end_time]', (isset($times) ? $times : null), (isset($vars->schedules[$i]['end_time']) ? $vars->schedules[$i]['end_time'] : null), ['class' => 'form-select time']);?></td>
                            </tr>
                            <?php
                                $i++;
                            }
                            unset($i);
                            ?>
                            </tbody>
                        </table>
                        </div>

                        <h6 class="form-section-header"><?php $this->_('AdminStaff.edit.heading_settings');?></h6>
                        <div class="mb-3">
                            <label class="form-label">
                                <?php $this->_('AdminStaff.edit.ticket_emails');?>
                                <i class="bi bi-info-circle ms-1" data-bs-toggle="tooltip" data-bs-placement="top" title="<?php echo $this->Html->safe($this->_('AdminStaff.!tooltip.ticket_emails', true));?>"></i>
                            </label>
                            <div>
                            <?php
                            // Set checkboxes for each priority
                            foreach ((isset($priorities) ? $priorities : []) as $priority => $language) {
                                $setting_value = isset($vars->settings['ticket_emails'][$priority]) ? $vars->settings['ticket_emails'][$priority] : '';
                            ?>
                                <div class="form-check form-check-inline">
                                    <?php
                                    $this->Form->fieldCheckbox('settings[ticket_emails][' . $priority . ']', 'true', ($setting_value == 'true'), ['id' => 'ticket_emails_' . $priority, 'class' => 'form-check-input']);
                                    ?>
                                    <label class="form-check-label" for="ticket_emails_<?php echo $this->Html->safe($priority);?>"><?php echo $this->Html->safe($language);?></label>
                                </div>
                            <?php
                            }
                            ?>
                            </div>
                        </div>
                        <div class="mb-3">
                            <label class="form-label">
                                <?php $this->_('AdminStaff.edit.mobile_ticket_emails');?>
                                <i class="bi bi-info-circle ms-1" data-bs-toggle="tooltip" data-bs-placement="top" title="<?php echo $this->Html->safe($this->_('AdminStaff.!tooltip.mobile_ticket_emails', true));?>"></i>
                            </label>
                            <div>
                            <?php
                            // Set checkboxes for each priority
                            foreach ((isset($priorities) ? $priorities : []) as $priority => $language) {
                                $setting_value = isset($vars->settings['mobile_ticket_emails'][$priority]) ? $vars->settings['mobile_ticket_emails'][$priority] : '';
                            ?>
                                <div class="form-check form-check-inline">
                                    <?php
                                    $this->Form->fieldCheckbox('settings[mobile_ticket_emails][' . $priority . ']', 'true', ($setting_value == 'true'), ['id' => 'mobile_ticket_emails_' . $priority, 'class' => 'form-check-input']);
                                    ?>
                                    <label class="form-check-label" for="mobile_ticket_emails_<?php echo $this->Html->safe($priority);?>"><?php echo $this->Html->safe($language);?></label>
                                </div>
                            <?php
                            }
                            ?>
                            </div>
                        </div>
                        <div class="mb-3">
                            <label class="form-label">
                                <?php $this->_('AdminStaff.edit.ticket_messenger_notifications');?>
                                <i class="bi bi-info-circle ms-1" data-bs-toggle="tooltip" data-bs-placement="top" title="<?php echo $this->Html->safe($this->_('AdminStaff.!tooltip.ticket_messenger_notifications', true));?>"></i>
                            </label>
                            <div>
                            <?php
                            // Set checkboxes for each priority
                            foreach ((isset($priorities) ? $priorities : []) as $priority => $language) {
                                $setting_value = isset($vars->settings['ticket_messenger_notifications'][$priority]) ? $vars->settings['ticket_messenger_notifications'][$priority] : '';
                            ?>
                                <div class="form-check form-check-inline">
                                    <?php
                                    $this->Form->fieldCheckbox('settings[ticket_messenger_notifications][' . $priority . ']', 'true', ($setting_value == 'true'), ['id' => 'ticket_messenger_notifications_' . $priority, 'class' => 'form-check-input']);
                                    ?>
                                    <label class="form-check-label" for="ticket_messenger_notifications_<?php echo $this->Html->safe($priority);?>"><?php echo $this->Html->safe($language);?></label>
                                </div>
                            <?php
                            }
                            ?>
                            </div>
                        </div>
                        <div class="mb-3">
                            <label for="settings_title" class="form-label">
                                <?php $this->_('AdminStaff.edit.title');?>
                                <i class="bi bi-info-circle ms-1" data-bs-toggle="tooltip" data-bs-placement="top" title="<?php echo $this->Html->safe($this->_('AdminStaff.!tooltip.title', true, $this->_('AdminTickets.reply.staff_title', true)));?>"></i>
                            </label>
                            <?php $this->Form->fieldText('settings[title]', (isset($vars->settings['title']) ? $vars->settings['title'] : null), ['id' => 'settings_title', 'class' => 'form-control']);?>
                        </div>
                        <div class="mb-3">
                            <label for="settings_signature" class="form-label">
                                <?php $this->_('AdminStaff.edit.signature');?>
                                <i class="bi bi-info-circle ms-1" data-bs-toggle="tooltip" data-bs-placement="top" title="<?php echo $this->Html->safe($this->_('AdminStaff.!tooltip.signature', true));?>"></i>
                            </label>
                            <?php $this->Form->fieldTextarea('settings[signature]', (isset($vars->settings['signature']) ? $vars->settings['signature'] : null), ['id' => 'settings_signature', 'data-markdown-editor' => '']);?>
                        </div>
                        <div class="mb-3">
                            <label for="settings_markdown_editor_mode" class="form-label">
                                <?php $this->_('AdminStaff.edit.markdown_editor_mode');?>
                                <i class="bi bi-info-circle ms-1" data-bs-toggle="tooltip" data-bs-placement="top" title="<?php echo $this->Html->safe($this->_('AdminStaff.!tooltip.markdown_editor_mode', true));?>"></i>
                            </label>
                            <?php
                            $markdown_modes = [
                                'wysiwyg' => 'WYSIWYG',
                                'markdown' => 'Markdown',
                                'markdown_no_preview' => 'Markdown - No Preview'
                            ];
                            $this->Form->fieldSelect('settings[markdown_editor_mode]', $markdown_modes, (isset($vars->settings['markdown_editor_mode']) ? $vars->settings['markdown_editor_mode'] : 'wysiwyg'), ['id' => 'settings_markdown_editor_mode', 'class' => 'form-select']);
                            ?>
                        </div>

                        <h6 class="form-section-header"><?php $this->_('AdminStaff.edit.heading_departments');?></h6>
                        <div class="dual-select-container mb-4">
                            <div class="dual-select-panel">
                                <label class="form-label"><?php $this->_('AdminStaff.edit.text_assigneddepartments');?></label>
                                <div class="dual-select-dropzone">
                                    <div class="drag-list" id="assigned_departments">
                                        <?php
                                        if (!empty($vars->departments)) {
                                            foreach ($vars->departments as $dept_id => $dept_name) {
                                        ?>
                                        <div class="drag-item" data-value="<?php echo $this->Html->safe($dept_id);?>" draggable="true"><?php echo $this->Html->safe($dept_name);?></div>
                                        <?php
                                            }
                                        }
                                        ?>
                                    </div>
                                </div>
                                <?php $this->Form->fieldMultiSelect('departments[]', [], [], ['id' => 'assigned', 'style' => 'display:none;']);?>
                            </div>

                            <div class="dual-select-controls">
                                <button type="button" class="btn btn-sm btn-outline-primary dual-select-btn move_left" title="<?php $this->_('AdminStaff.edit.text_assigneddepartments');?>">
                                    <i class="bi bi-arrow-left"></i>
                                </button>
                                <button type="button" class="btn btn-sm btn-outline-primary dual-select-btn move_right" title="<?php $this->_('AdminStaff.edit.text_availabledepartments');?>">
                                    <i class="bi bi-arrow-right"></i>
                                </button>
                            </div>

                            <div class="dual-select-panel">
                                <label class="form-label"><?php $this->_('AdminStaff.edit.text_availabledepartments');?></label>
                                <div class="dual-select-dropzone">
                                    <div class="drag-list" id="available_departments">
                                        <?php
                                        if (!empty($departments)) {
                                            foreach ($departments as $dept_id => $dept_name) {
                                        ?>
                                        <div class="drag-item" data-value="<?php echo $this->Html->safe($dept_id);?>" draggable="true"><?php echo $this->Html->safe($dept_name);?></div>
                                        <?php
                                            }
                                        }
                                        ?>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div><!-- end card-body -->
                    <div class="card-footer d-flex justify-content-end">
                        <?php
                        $this->Form->fieldSubmit('save', $this->_('AdminStaff.edit.field_editsubmit', true), ['class' => 'btn btn-primary']);
                        ?>
                    </div>
                </div><!-- end card-content -->
                <?php
                $this->Form->end();
                ?></section>

<script type="text/javascript">
document.addEventListener('DOMContentLoaded', function() {
    // Initialize Bootstrap tooltips
    var tooltipTriggerList = document.querySelectorAll('[data-bs-toggle="tooltip"]');
    tooltipTriggerList.forEach(function(tooltipTriggerEl) {
        new bootstrap.Tooltip(tooltipTriggerEl);
    });

    var staffForm = document.getElementById('staff');
    var hiddenSelect = document.getElementById('assigned');

    // Drag & Drop Functionality for Departments
    var assignedDepartmentsList = document.getElementById('assigned_departments');
    var availableDepartmentsList = document.getElementById('available_departments');
    var moveLeftBtn = document.querySelector('.move_left');
    var moveRightBtn = document.querySelector('.move_right');

    var draggedItem = null;

    function setupDragItems() {
        document.querySelectorAll('#assigned_departments .drag-item, #available_departments .drag-item').forEach(function(item) {
            // Click to select
            item.addEventListener('click', function(e) {
                e.stopPropagation();
                if (e.ctrlKey || e.metaKey) {
                    this.classList.toggle('selected');
                } else {
                    document.querySelectorAll('.drag-item').forEach(function(i) {
                        i.classList.remove('selected');
                    });
                    this.classList.add('selected');
                }
            });

            // Drag events
            item.addEventListener('dragstart', function(e) {
                draggedItem = this;
                this.classList.add('dragging');
                e.dataTransfer.effectAllowed = 'move';
                e.dataTransfer.setData('text/plain', this.textContent);
            });

            item.addEventListener('dragend', function() {
                this.classList.remove('dragging');
                document.querySelectorAll('.drag-list').forEach(function(l) {
                    l.classList.remove('drag-over');
                });
            });
        });

        // Setup drop zones
        [assignedDepartmentsList, availableDepartmentsList].forEach(function(list) {
            if (!list) return;

            list.addEventListener('dragover', function(e) {
                e.preventDefault();
                this.classList.add('drag-over');
            });

            list.addEventListener('dragleave', function(e) {
                if (!this.contains(e.relatedTarget)) {
                    this.classList.remove('drag-over');
                }
            });

            list.addEventListener('drop', function(e) {
                e.preventDefault();
                this.classList.remove('drag-over');

                if (draggedItem && draggedItem.parentNode !== this) {
                    this.appendChild(draggedItem);
                    draggedItem.classList.remove('selected');
                    draggedItem = null;
                }
            });
        });
    }

    setupDragItems();

    // Arrow button functionality
    if (moveLeftBtn) {
        moveLeftBtn.addEventListener('click', function() {
            var selectedItems = availableDepartmentsList.querySelectorAll('.drag-item.selected');
            if (selectedItems.length > 0) {
                selectedItems.forEach(function(item) {
                    assignedDepartmentsList.appendChild(item);
                    item.classList.remove('selected');
                });
            }
        });
    }

    if (moveRightBtn) {
        moveRightBtn.addEventListener('click', function() {
            var selectedItems = assignedDepartmentsList.querySelectorAll('.drag-item.selected');
            if (selectedItems.length > 0) {
                selectedItems.forEach(function(item) {
                    availableDepartmentsList.appendChild(item);
                    item.classList.remove('selected');
                });
            }
        });
    }

    // Clear selection when clicking outside
    document.addEventListener('click', function(e) {
        if (!e.target.closest('.drag-item')) {
            document.querySelectorAll('.drag-item.selected').forEach(function(item) {
                item.classList.remove('selected');
            });
        }
    });

    // Update hidden select on form submit
    if (staffForm) {
        staffForm.addEventListener('submit', function() {
            var items = assignedDepartmentsList.querySelectorAll('.drag-item');

            // Clear existing options
            hiddenSelect.innerHTML = '';

            // Add all assigned departments as selected options
            items.forEach(function(item) {
                var option = document.createElement('option');
                option.value = item.getAttribute('data-value');
                option.selected = true;
                hiddenSelect.appendChild(option);
            });
        });
    }

    // Handle all day checkbox - disable time fields
    document.querySelectorAll('#staff .all_day').forEach(function(checkbox) {
        // Initial state
        if (checkbox.checked) {
            var row = checkbox.closest('tr');
            var timeSelects = row.querySelectorAll('.time');
            timeSelects.forEach(function(select) {
                select.value = '00:00:00';
                select.disabled = true;
            });
        }

        // On change
        checkbox.addEventListener('change', function() {
            var row = this.closest('tr');
            var timeSelects = row.querySelectorAll('.time');
            if (this.checked) {
                timeSelects.forEach(function(select) {
                    select.value = '00:00:00';
                    select.disabled = true;
                });
            } else {
                timeSelects.forEach(function(select) {
                    select.value = '';
                    select.disabled = false;
                });
            }
        });
    });

    // Markdown editor mode handling
    var editorModeSelect = document.getElementById('settings_markdown_editor_mode');
    var signatureTextarea = document.getElementById('settings_signature');

    function applyEditorMode(mode) {
        // Wait for editor to be initialized
        setTimeout(function() {
            var editorContainer = signatureTextarea.nextElementSibling;
            if (editorContainer && editorContainer._editor) {
                var editor = editorContainer._editor;
                if (mode === 'markdown_no_preview') {
                    editor.changeMode('markdown');
                    if (editor.commandManager) {
                        editor.commandManager.exec('togglePreview');
                    }
                } else if (mode === 'markdown') {
                    editor.changeMode('markdown');
                } else {
                    editor.changeMode('wysiwyg');
                }
            }
        }, 500);
    }

    if (editorModeSelect) {
        applyEditorMode(editorModeSelect.value || 'wysiwyg');

        editorModeSelect.addEventListener('change', function() {
            applyEditorMode(this.value);
        });
    }
});
</script>
